"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const engine_1 = require("../compiler/transpiler/engine");
async function testIntegration() {
    console.log("🧪 RUNNING SOVEREIGN INTEGRATION TEST...");
    const mockArtifact = {
        uid: "SOV-999",
        name: "core_logic.ts",
        type: 'CODE',
        version: '1.0.0',
        content: "console.log('Sovereign Executing...');",
        checksum: "abc",
        createdAt: Date.now()
    };
    try {
        const source = engine_1.TranspilerEngine.transpileToSource(mockArtifact);
        console.log("✅ [PASS] Transpilation & Security Check");
        if (!source.includes("GENERATED BY FEAC"))
            throw new Error("Missing Header");
        console.log("✅ [PASS] Industrial Header Injection");
    }
    catch (e) {
        console.error("❌ [FAIL] Integration Test:", e);
        process.exit(1);
    }
    console.log("✨ ALL INTEGRATION TESTS PASSED.");
}
testIntegration();
